# Copy this file to `.env` at the repo root and adjust the values
# for your network. The real `.env` is gitignored and is copied into
# the app bundle at build time by the "Copy .env into bundle" phase.
BDP_IP=192.168.1.150
PORT=50001
DEVICE_ID=MediaRemote:11-22-33-44-55-66
DEVICE_NAME=iOS_Custom_Remote
